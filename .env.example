# =============================================================================
# InSite Signs - Environment Configuration
# =============================================================================
# SECURITY WARNING: This file is public. DO NOT put real secrets here.
# Copy this file to .env and fill in real values.
#
# Environment: dev, staging, prod
APP_STAGE=dev
FLASK_ENV=development

# Base URL (for webhooks/callbacks)
BASE_URL=http://127.0.0.1:5000

# -----------------------------------------------------------------------------
# Database (PostgreSQL ONLY)
# -----------------------------------------------------------------------------
# Format: postgresql://user:password@localhost:5432/dbname
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/insitesigns

# -----------------------------------------------------------------------------
# Security & Auth
# -----------------------------------------------------------------------------
SECRET_KEY=dev-secret-key-change-this-in-prod
PRINT_JOBS_TOKEN=dev-print-token

# -----------------------------------------------------------------------------
# Stripe Integration
# -----------------------------------------------------------------------------
# Use Test keys (sk_test_...) for local dev
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Subscription Price IDs (Created in Stripe Dashboard)
STRIPE_PRICE_MONTHLY=price_monthly_id
STRIPE_PRICE_ANNUAL=price_annual_id
STRIPE_PRICE_ID_PRO=price_pro_id

# -----------------------------------------------------------------------------
# Storage (Local vs S3)
# -----------------------------------------------------------------------------
# Options: local, s3
STORAGE_BACKEND=local

# AWS S3 (Required if STORAGE_BACKEND=s3)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
S3_BUCKET=my-signs-bucket
S3_PREFIX=dev/

# -----------------------------------------------------------------------------
# Paths (Optional overrides, defaults usually work)
# -----------------------------------------------------------------------------
# INSTANCE_DIR=./instance

# -----------------------------------------------------------------------------
# Feature Flags / Limits
# -----------------------------------------------------------------------------
# Free Tier Limits
FREE_TIER_MAX_PHOTOS=5
FREE_TIER_MAX_ACTIVE_PROPERTIES=1
FREE_TIER_RETENTION_DAYS=7

# -----------------------------------------------------------------------------
# Hybrid Monetization & Limits
# -----------------------------------------------------------------------------
STRIPE_PRICE_LISTING_UNLOCK=price_1SmW...
STRIPE_PRICE_LISTING_KIT=price_...

# -----------------------------------------------------------------------------
# Print Productization (Phase 6)
# -----------------------------------------------------------------------------
# Print pricing uses Stripe Price lookup_keys; no env vars.
# Ensure Products and Prices with correct lookup_keys are created in Stripe.
